const mindee = require('mindee'); async function test() { const client = new mindee.v1.Client({ apiKey: process.env.MINDEE_API_KEY }); const inputSource = new mindee.Base64Input({ inputString: Buffer.from('test').toString('base64'), filename: 'test.pdf' }); try { await client.parse(mindee.v1.product.invoice.InvoiceV4, inputSource); } catch(e) { console.log(e.message) } } test();  
